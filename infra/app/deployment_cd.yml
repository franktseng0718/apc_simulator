apiVersion: apps/v1
kind: Deployment
metadata:
  name: apc-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apc-simulator
  template:
    metadata:
      labels:
        app: apc-simulator
    spec:
      containers:
        - name: apc-simulator
          image: apc-simulator
          ports:
            - containerPort: 3080
          env:
            - name: PORT
              value: '3080'
            - name: NATS_SERVICE_CONNECTION
              value: 'nats-server:4220'
